#ifndef _random_lib_h
#define _random_lib_h

unsigned int random();
unsigned int random(unsigned int i);
float random(float f);
unsigned int R_rnd();
void R_mize(unsigned int start = time(NULL));

#endif
